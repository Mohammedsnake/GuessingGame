{
  "name": "nexiq",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^10.11.1"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "vite": "^5.1.4"
  }
}